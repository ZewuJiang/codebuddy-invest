# 2026-05-23 工作日志

## 19:30 - 投研鸭小程序迁移微信云开发 → 自建腾讯云服务器

- **需求**：微信云开发环境到期，小程序数据全部显示空白，需迁移到自有服务器
- **产出**：
  - `touyanduck_appid/app.js`：v5.0 → v6.0，移除云开发初始化，改为 HTTP API 配置（`apiBaseUrl: https://miniapp.touyanduck.com/api`）
  - `touyanduck_appid/services/api.js`：v7.0 → v8.0，将 `wx.cloud.database()` 全面替换为 `wx.request()` HTTP 请求
  - `touyanduck_appid/app.json`：移除 `"cloud": true` 字段
- **服务器配置**：
  - 服务器：腾讯云轻量 `lhins-1x1cx0o1`，IP `119.91.74.175`，广州，到期 2026-07-08
  - 子域名：`miniapp.touyanduck.com`（DNSPod A记录 → 119.91.74.175）
  - 宝塔面板：安装 Nginx 1.28，创建网站，申请 Let's Encrypt SSL（到期 2026-08-21），强制 HTTPS
  - 数据目录：`/www/wwwroot/miniapp.touyanduck.com/api/`（含 briefing/markets/watchlist/radar.json）
  - 微信公众平台：request 合法域名已添加 `https://miniapp.touyanduck.com`
- **关键决策**：使用纯静态 JSON 文件 + Nginx 托管，无需后端代码或数据库，架构简单零成本
- **验证结果**：小程序模拟器四个页面（简报/市场/标的/雷达）数据全部正常显示
## 20:05 - 全链路检查与 Skill 规范同步

- **需求**：确保从 Skill 到服务器到小程序的完整链路无遗漏，清理旧的微信云开发冗余描述
- **产出**：
  - `SKILL.md`：v13.1 → v14.0，更新 description/版本快照/Phase3分发通道描述
  - `CHANGELOG.md`：新增 v14.0 条目，完整记录本次迁移内容
  - `run_daily.sh` 注释：已对齐 v6.6
- **验证结果**：4 个 JSON 接口全部 HTTP 200，SSH 免密登录正常，服务器文件时间戳正确

## 20:15 - Skill 冗余清理（移除微信云开发全部残留）

- **需求**：清除 touyanduck-daily Skill 中所有微信云开发残留，保持 Skill 精炼，减少 AI 加载时的冗余上下文
- **产出**：
  - 删除 `scripts/upload_to_cloud.py`（废弃云上传脚本）
  - 删除 `scripts/__pycache__/`（含 upload_to_cloud 等5个 .pyc 缓存）
  - 重写 `README.md`（v13.1 → v14.0，移除"配置微信云数据库"步骤3/4，文件结构表同步更新）
  - `references/known-pitfalls.md`：删除上传堵点中 access_token/云数据库写入/upload_to_cloud清洗3条，更新堵点#41引用
  - `references/daily-checklist.md`：P4/V1/V2b/故障排查全面改为自建服务器表述
  - `references/templates.md`：云数据库上传 → 自建服务器上传（HTTP 200验证）
  - `references/weekend-mode.md`：第三阶段描述更新
  - `references/json-schema.md`：删除 v1.4 语音播报字段定义（audioUrl/audioFile/voiceText，v13.0已永久移除）
  - `scripts/run_daily.sh`：删除"原 upload_to_cloud.py 保留但不调用"过时注释
- **关键决策**：严格只清理废弃云开发残留，不触碰任何现有工作流逻辑，零回归风险
