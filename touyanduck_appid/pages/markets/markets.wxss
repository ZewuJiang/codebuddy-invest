/* pages/markets/markets.wxss */
/* 市场页样式 v4.4 — Tab圆点indicator + VIX反色 + CSS清理 */

.page-container {
  padding-bottom: 20rpx;
}

/* ========== 顶部Tab ========== */
.tab-bar {
  display: flex;
  background: #ffffff;
  padding: 16rpx 24rpx;
  border-bottom: 1rpx solid #f0f0f0;
  position: sticky;
  top: 0;
  z-index: 10;
}

.tab-item {
  flex: 1;
  text-align: center;
  font-size: 26rpx;
  color: #999;
  padding: 12rpx 0;
  position: relative;
  transition: color 0.3s ease;
  z-index: 1;
}

.tab-active {
  color: #FFB800;
  font-weight: 600;
}

.tab-indicator {
  position: absolute;
  bottom: 6rpx;
  left: 24rpx;
  width: calc((100% - 48rpx) / 5);
  height: 10rpx;
  transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  display: flex;
  justify-content: center;
}

.tab-indicator::after {
  content: '';
  width: 10rpx;
  height: 10rpx;
  background: linear-gradient(135deg, #FFB800, #FF8C00);
  border-radius: 50%;
}

/* ========== Swiper ========== */
.market-swiper {
  width: 100%;
}

.market-scroll {
  height: 100%;
}

/* ========== Tab内容包裹（SelectorQuery量高用） ========== */
.tab-content-wrap {
  padding-bottom: 24rpx;
}

/* ========== 每Tab摘要条 ========== */
.market-summary {
  margin: 16rpx 24rpx 0;
  padding: 16rpx 20rpx;
  background: linear-gradient(135deg, rgba(255, 184, 0, 0.08), rgba(255, 140, 0, 0.04));
  border-radius: 12rpx;
  border-left: 4rpx solid #FFB800;
}

.summary-bar {
  font-size: 26rpx;
  color: #555;
  line-height: 1.7;
}

.summary-hl {
  color: #D4740A;
  font-weight: 600;
}

/* ========== 列表卡片 ========== */
.market-list-card {
  background: #ffffff;
  border-radius: 16rpx;
  margin: 16rpx 24rpx;
  box-shadow: 0 2rpx 16rpx rgba(0, 0, 0, 0.06);
  overflow: hidden;
}

.market-list-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20rpx 24rpx;
  border-bottom: 1rpx solid #f5f5f5;
  transition: all 0.15s ease;
}

.market-list-item:last-child {
  border-bottom: none;
}

.market-list-item:active {
  background: #f0f0f0;
  transform: scale(0.985);
}

.ml-left {
  flex: 1;
  min-width: 0;
}

.ml-name {
  font-size: 28rpx;
  font-weight: 600;
  color: #1a1a2e;
  margin-bottom: 4rpx;
}

.ml-price {
  font-size: 24rpx;
  color: #888;
}

.ml-center {
  flex-shrink: 0;
  margin: 0 16rpx;
  display: flex;
  align-items: center;
}

.ml-right {
  text-align: right;
  min-width: 110rpx;
  flex-shrink: 0;
}

.ml-change-badge {
  font-size: 28rpx;
  font-weight: 700;
}

.ml-label {
  font-size: 20rpx;
  color: #999;
  margin-top: 4rpx;
}

/* ========== M7巨头头部 ========== */
.m7-header {
  padding: 24rpx 32rpx 8rpx;
}

.m7-title {
  font-size: 30rpx;
  font-weight: 700;
  color: #1a1a2e;
  display: block;
}

/* ========== GICS板块热力图 ========== */
.gics-section {
  margin: 16rpx 24rpx;
  background: #fff;
  border-radius: 16rpx;
  padding: 20rpx 24rpx;
  box-shadow: 0 2rpx 16rpx rgba(0, 0, 0, 0.06);
}

.gics-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #1a1a2e;
  margin-bottom: 16rpx;
  padding-left: 12rpx;
  border-left: 4rpx solid #FFB800;
}

.gics-grid {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.gics-item {
  display: flex;
  align-items: center;
  padding: 10rpx 0;
}

.gics-name {
  font-size: 24rpx;
  color: #555;
  width: 200rpx;
  flex-shrink: 0;
}

.gics-bar {
  flex: 1;
  height: 16rpx;
  background: #f5f5f5;
  border-radius: 8rpx;
  overflow: hidden;
  margin: 0 16rpx;
}

.gics-bar-fill {
  height: 100%;
  border-radius: 8rpx;
  transition: width 0.8s ease;
  max-width: 100%;
}

.bar-up {
  background: linear-gradient(90deg, rgba(231, 76, 60, 0.3), #e74c3c);
}

.bar-down {
  background: linear-gradient(90deg, rgba(39, 174, 96, 0.3), #27ae60);
}

.gics-change {
  font-size: 24rpx;
  font-weight: 600;
  min-width: 100rpx;
  text-align: right;
  flex-shrink: 0;
}

/* data-footer / footer-text 已统一到 app.wxss */

/* ========== 入场动画 ========== */
.stagger-item {
  opacity: 0;
  transform: translateX(30rpx);
}

.stagger-show {
  animation: slideInRight 0.4s ease forwards;
}

@keyframes slideInRight {
  from {
    opacity: 0;
    transform: translateX(30rpx);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

/* ========== GICS 一句话轮动摘要 ========== */
.gics-summary {
  margin-bottom: 16rpx;
  padding: 12rpx 16rpx;
  background: linear-gradient(135deg, rgba(255, 184, 0, 0.08), rgba(255, 140, 0, 0.04));
  border-radius: 10rpx;
  border-left: 3rpx solid #FFB800;
}

.gics-summary .summary-bar {
  font-size: 24rpx;
  color: #555;
  line-height: 1.65;
}
